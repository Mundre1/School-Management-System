# Minimal requirements for initial setup
# Install these first, then install full requirements.txt

# Core Django
Django==4.2.7
djangorestframework==3.14.0
python-decouple==3.8

# Database (will install after PostgreSQL is installed)
# psycopg2-binary==2.9.9

# Authentication
djangorestframework-simplejwt==5.3.0
django-cors-headers==4.3.1

# Static Files
whitenoise==6.6.0

# Image Processing
Pillow==10.1.0

# API Documentation
drf-yasg==1.21.7
